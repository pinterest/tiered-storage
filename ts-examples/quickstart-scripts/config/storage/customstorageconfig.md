# Add your custom storage configuration here

Details in [Quickstart Guide](../../../../docs/quickstart.md), step 2: `Run Segment Uploader`.

Note: Files ending with `.json` in this directory will be ignored by .gitignore.